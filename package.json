{
  "name": "plaintext-article",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "postinstall": "npm run build",
    "start": "npm run build && npx --yes serve . -p 3000"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "pdfkit": "^0.15.0"
  }
}
